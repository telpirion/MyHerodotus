name: Docker

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
    - name: Build Docker image
      run: |
        docker build . -t myherodotus -f Dockerfile
      working-directory: .
